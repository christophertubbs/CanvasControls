/**
 * @file Defines a series of named blocks that may be toggled by any number of contained tab fields.
 * This differs from a FieldSet in that only one tab block is shown at a time where a FieldSet is 
 * only one element that will be shown when revealed.
 * 
 * @see {@link Block.js}
 * 
 * @author Christopher Tubbs
 */